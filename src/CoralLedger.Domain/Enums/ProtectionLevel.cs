namespace CoralLedger.Domain.Enums;

public enum ProtectionLevel
{
    NoTake,           // Full protection - no extractive activities
    HighlyProtected,  // Minimal extractive activities allowed
    LightlyProtected, // Some sustainable fishing allowed
    MinimalProtection // Basic regulations only
}
